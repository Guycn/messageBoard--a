#include "Board.hpp"
#include "Direction.hpp"
using ariel::Direction;

#include <iostream>
#include <stdexcept>
using namespace std;
using namespace ariel;
const unsigned int MAX_NUM = 1000000;

int main() {
	ariel::Board board;
	//board.post(0, 0, Direction::Vertical, "xyz");
    //cout << board.read(0, 0, Direction::Horizontal, 4) << endl; 
    //board.show(); // shows the board in a reasonable way. For example: 

}